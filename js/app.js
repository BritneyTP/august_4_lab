'use strict';


// This would need more thought - but we decided to work on GitHub processes and be thorough there rather than JS logic.
// I did learn that tasks take more time.

function init() {
  input = document.getElementById('input');
}

function convert_inches_centimeters() {
  // if the input is inches - convert to cm
  // if the input is centimeters - convert to inches
}
